# ── Core Web Framework ──────────────────────────────────────────────────────
fastapi>=0.110.1                # FastAPI framework — used in all API routes and main.py
uvicorn>=0.25.0                 # ASGI server to run FastAPI app
python-multipart>=0.0.9         # Required by FastAPI for form/file upload parsing

# ── Database ─────────────────────────────────────────────────────────────────
motor>=3.3.1                    # Async MongoDB driver — used in database.py and all repositories
pymongo>=4.5.0                  # Sync PyMongo — used directly in seed.py, scripts, ReplaceOne/UpdateOne

# ── Validation & Settings ────────────────────────────────────────────────────
pydantic>=2.6.4                 # Data validation & serialization — used in all DTOs and models
email-validator>=2.2.0          # Enables pydantic.EmailStr — used in userDTO.py
python-dotenv>=1.0.1            # Load .env config — used in ai_config.py, database.py, seed scripts
typing-extensions>=4.0.0        # TypedDict & advanced typing — imported as `typing_extensions as typing` in chatDTO.py

# ── Authentication & Security ────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0  # JWT encode/decode — used in core/auth.py (jose.jwt, jose.JWTError)
passlib[bcrypt]>=1.7.4            # Password hashing with bcrypt backend — used in core/auth.py (CryptContext)

# ── AI / Google GenAI ────────────────────────────────────────────────────────
google-genai>=1.0.0             # New Gemini SDK (google.genai) — used in ai_config.py, ragRepository.py, chatService.py
google-generativeai>=0.8.0      # Legacy Gemini SDK (google.generativeai) — used in seed.py and debug scripts

# ── Async HTTP & Caching ─────────────────────────────────────────────────────
httpx>=0.27.0                   # Async HTTP client — used in test_chat_stream.py (streaming tests)
redis>=7.4.0                    # Redis async client — imported as `redis.asyncio` in ai_config.py for caching

# ── Cloud Storage ─────────────────────────────────────────────────────────────
cloudinary>=1.40.0              # Cloudinary upload signing — used in uploadAPI.py (cloudinary.utils)

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0                   # Test runner — used in tests/ directory
pytest-asyncio>=0.23.0          # Async test support for pytest

# ── Dev Tooling (linting / formatting / type-checking) ───────────────────────
black>=24.1.1                   # Code formatter
isort>=5.13.2                   # Import sorter
flake8>=7.0.0                   # Linter
mypy>=1.8.0                     # Static type checker
